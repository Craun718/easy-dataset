# Easy Dataset 数据集创建流程

## 概述

Easy Dataset 是一个专为大型语言模型（LLM）微调数据集创建而设计的应用程序。本文档详细介绍从 PDF 文件中提取文本块、生成问答对、验证数据集的完整流程。

```
创建项目 → 配置模型 → 配置分块参数 → 上传PDF → 生成问题 → 生成答案 → 验证数据集 → 导出数据集
```

---

## 第一步：创建项目

在主页创建新项目，填写项目名称和描述。

---

## 第二步：配置模型

在模型设置页面，添加并配置至少一个模型提供商，执行健康检查确认模型可用。

### 支持的模型提供商

| 提供商 ID | 名称 |
|-----------|------|
| `ollama` | Ollama（本地部署） |
| `openai` | OpenAI |
| `siliconcloud` | 硅基流动 |
| `deepseek` | DeepSeek |
| `302ai` | 302.AI |
| `zhipu` | 智谱AI |
| `Doubao` | 火山引擎 |
| `groq` | Groq |
| `grok` | Grok |
| `OpenRouter` | OpenRouter |
| `alibailian` | 阿里云百炼 |
| `minimax` | MiniMax |

### 模型健康检查

系统提供模型端点健康检查功能，用于验证模型配置是否正确可用：

1. 系统向模型的 API 端点发送测试请求
2. 验证端点是否可达、API 密钥是否有效
3. 获取模型列表，确认当前选择的模型是否存在于列表中
4. 返回检查结果，显示健康或异常状态及详细信息

| 状态 | 图标颜色 | 含义 |
|------|----------|------|
| 未检测 | 灰色 | 尚未执行健康检查 |
| 检测中 | 灰色 | 正在检测端点连通性 |
| 健康 | 绿色 | 端点可达，模型可用 |
| 异常 | 红色 | 端点不可达或配置错误 |

---

## 第三步：配置分块参数

在文本分割设置页面，根据文档特点选择合适的分块方法并调整参数。

系统提供 **5 种**文本分块方法：

### 3.1 智能 Markdown 分块（默认方法）

> 当未显式指定分块类型时，系统自动使用此方法。

基于 Markdown 文档的标题层级结构进行智能分块。处理过程：扫描全文提取标题构建大纲 → 按标题边界拆分段落 → 合并短段落、拆分超长段落。该方法会自动为每个块生成摘要。

| 参数名 | 说明 | 默认值 | 建议范围 |
|--------|------|--------|----------|
| `textSplitMinLength` | 块的最小字符数，低于此值会与相邻段落合并 | 1500 | 500 - 3000 |
| `textSplitMaxLength` | 块的最大字符数，超过此值会被进一步拆分 | 2000 | 1000 - 5000 |

### 3.2 字符分块（`text`）

按指定分隔符切分文本，相邻块可合并以减少碎片。不理解文档结构，适合简单纯文本。

| 参数名 | 说明 | 默认值 | 建议范围 |
|--------|------|--------|----------|
| `chunkSize` | 每个块的目标字符数 | 1500 | 500 - 5000 |
| `chunkOverlap` | 相邻块重叠的字符数 | 200 | 0 - 500 |
| `separator` | 分隔符 | `\n\n` | `\n`、`。` 等 |

### 3.3 Token 分块（`token`）

按 Token 数量分块，生成的每个块消耗的 Token 数量精确可控，直接对应 LLM 的输入 Token 消耗。

| 参数名 | 说明 | 默认值 | 建议范围 |
|--------|------|--------|----------|
| `chunkSize` | 每个块的目标 Token 数量 | 1500 | 500 - 8000 |
| `chunkOverlap` | 相邻块重叠的 Token 数量 | 200 | 0 - 500 |

### 3.4 递归分块（`recursive`）

使用多级分隔符列表进行递归分割：先尝试第一优先级分隔符，若块仍超长则使用下一优先级分隔符继续分割。

| 参数名 | 说明 | 默认值 | 建议范围 |
|--------|------|--------|----------|
| `chunkSize` | 每个块的目标字符数 | 1500 | 500 - 5000 |
| `chunkOverlap` | 相邻块重叠的字符数 | 200 | 0 - 500 |
| `separators` | 多级分隔符列表，按优先级从高到低排列 | `['\|', '##', '>', '-']` | 见下文 |

**分隔符列表设计：**

列表应从"最强边界"到"最弱边界"排列：

- **Markdown 文档**：`['---', '##', '###', '\\n\\n', '\\n', '。', '.']`
- **一般文本**：`['\\n\\n', '\\n', '。', '！', '？', '.', '!', '?']`
- **混合内容**：`['|', '##', '>', '-', '\\n\\n', '\\n']`

### 3.5 自定义分块（`custom`）

用户指定任意字符串作为分隔符，严格按分隔符切割，不做合并或再分割。

| 参数名 | 说明 | 默认值 | 建议范围 |
|--------|------|--------|----------|
| `customSeparator` | 自定义分隔符 | `---` | 任意字符串 |

### 分块方法选择指南

**智能 Markdown 分块**是处理 Markdown 文档的首选方法。它能感知文档的标题层级结构，自动识别章节边界，并为每个块生成摘要。这是唯一支持自动生成摘要的方法，适合需要保留文档语义结构的场景。

**字符分块**是最简单的方法，按指定分隔符（如双换行）切分文本。它不理解文档结构，纯粹基于字符匹配，适合结构简单的纯文本或已预处理过的文本。

**Token 分块**按 Token 数量而非字符数量分块，每个块消耗的 Token 数量精确可控。当需要精确匹配 LLM 的上下文窗口限制时，这是最佳选择。

**递归分块**使用多级分隔符列表逐级分割，优先在更重要的边界处分割。它比字符分块更智能，但效果取决于分隔符列表的设计，适合结构化文档。

**自定义分块**允许用户指定任意分隔符，严格按分隔符切割。适合用户明确知道文档分隔模式的场景，如用特定标记分隔的多篇文章。

---

## 第四步：上传 PDF

进入项目页面，上传一个或多个 PDF 文件。系统会自动调用转换策略将 PDF 转为 Markdown，并使用已配置的分块参数进行文本分割。

系统提供 **4 种**PDF 处理策略，适用于不同类型的 PDF 文档：

### 4.1 默认转换（`default`）

直接使用 PDF 解析库提取文本内容，转换为 Markdown 格式。

**原理：** 解析 PDF 的文本层和基本布局，提取文字内容并转换为 Markdown 格式。

**适用场景：** 常规文档型 PDF，文字清晰、布局简单的文档。

**特点：**
- 无需额外配置，开箱即用
- 转换速度快
- 依赖 PDF 内嵌的文字层，扫描件或图片型 PDF 效果差
- 对复杂排版（多栏、表格）支持有限

### 4.2 MinerU 云端解析（`mineru`）

调用 MinerU 云端服务进行 PDF 解析，支持公式识别、表格提取和 OCR。

**原理：** 将 PDF 上传至 MinerU 云端服务，使用其文档布局分析模型和 VLM 引擎进行深度解析，返回结构化的 Markdown 内容。

**适用场景：** 复杂排版文档，如学术论文、技术报告、多栏报纸等。

**特点：**
- 支持公式识别
- 支持表格提取
- 支持 OCR 识别
- 解析质量高，能保留文档结构
- 需要配置 MinerU Token
- 需要网络连接，处理时间较长

**外部依赖：** MinerU 云服务（需要在 MinerU 官网注册并获取 Token）

### 4.3 MinerU 本地解析（`mineru-local`）

调用本地部署的 MinerU 服务进行 PDF 解析，功能与云端版相同，但数据不离开本地。

**原理：** 将 PDF 发送至本地部署的 MinerU 服务，使用本地 VLM 引擎进行解析。

**适用场景：** 对数据隐私有要求，或需要离线处理的场景。

**特点：**
- 功能与云端版一致
- 数据完全本地处理，不上传至云端
- 需要本地部署 MinerU 服务
- 处理速度取决于本地硬件性能

**外部依赖：** 本地 MinerU 服务（需要预先部署）

### 4.4 视觉模型转换（`vision`）

使用视觉大模型（如 Minimax、Kimi）逐页识别 PDF 内容，将其转换为 Markdown。

**原理：** 将 PDF 每页渲染为图片，发送给视觉大模型，由模型识别页面内容并生成 Markdown 文本。系统会先进行页面转文本，然后对生成的标题进行优化重组。

**适用场景：** 包含大量图片、图表、扫描件等视觉内容丰富的 PDF。

**特点：**
- 能识别图片、图表等非文字内容
- 理解复杂排版和视觉布局
- 转换质量最高，语义理解能力强
- 需要配置视觉模型
- 处理速度较慢，按页调用 API
- Token 消耗较大，成本较高

**外部依赖：** 支持视觉输入的 LLM 提供商（需要配置视觉模型）

### 处理策略选择指南

| 策略 | 解析质量 | 处理速度 | 成本 | 配置要求 |
|------|----------|----------|------|----------|
| 默认转换 | 一般 | 最快 | 免费 | 无 |
| MinerU 云端 | 高 | 中等 | 中等 | MinerU Token |
| MinerU 本地 | 高 | 较慢 | 免费 | 本地服务 |
| 视觉模型 | 最高 | 最慢 | 较高 | 视觉模型 |

---

## 第五步：生成问题

选择需要生成问题的文本块，配置使用的 AI 模型和语言参数，启动问题生成任务。

| 参数 | 说明 |
|------|------|
| 并发数 | 同时处理的文本块数量（默认 2） |
| 语言 | 生成问题的语言（中文/英文） |
| GA 扩展 | 是否基于"文体+受众"维度扩展问题多样性 |
| 问题数量 | 每个文本块生成的问题个数 |

每 1000 字建议生成 3-5 个问题，开启 GA 扩展可提升问题多样性，优先选择高质量模型以获得更好的问题。

### 问题质量约束

- 所有问题必须严格基于文本内容，不得添加外部信息
- 问题之间不得重复，需覆盖文本的不同侧面
- 禁止生成与文档元信息（作者、章节等）相关的问题
- 问题表述需自然流畅，不得包含"文献中提到"等引用性表述

---

## 第六步：生成答案

选择需要生成答案的问题（或全选），配置 AI 模型，启动答案生成任务。

| 参数 | 说明 |
|------|------|
| 并发数 | 同时处理的问题数量（默认 3） |
| 语言 | 生成答案的语言 |
| 模型配置 | 使用的模型提供商、模型名称、API 密钥等 |

复杂推理问题建议使用能力强的模型，简单事实性问题可使用经济型模型。启用思维链可提升答案的可解释性和质量。

### 答案生成流程

1. 获取待处理问题及其关联的文本块内容
2. 将文本块和问题嵌入答案生成提示词模板
3. 若问题关联了 GA pair（文体+受众），使用增强版提示词
4. 调用 LLM 获取答案和思维链（COT）
5. 若模型未返回思维链，系统自动基于问题、文本和答案合成人造思维链
6. 创建数据集条目，将问题标记为"已生成答案"

### 答案质量约束

- 答案必须完全基于提供的文本内容，不得虚构信息
- 答案必须准确回答问题的核心要求
- 答案需充分详细，包含所有必要信息
- 答案中不得出现"参考/依据/文献中提到"等引用性表述

---

## 第七步：验证数据集

选择数据集条目，启动 AI 评估任务，查看评分和评语，根据评分筛选或重新生成质量不达标的条目。

### 评估维度

| 维度 | 权重 | 评估内容 |
|------|------|----------|
| 问题质量 | 25% | 问题表述是否清晰、难度是否适中、语法是否正确 |
| 答案质量 | 35% | 答案是否准确完整、逻辑是否清晰、是否基于原文 |
| 文本相关性 | 25% | 问题和答案与原始文本的关联程度 |
| 整体一致性 | 15% | 问答之间是否形成逻辑闭环、是否适合模型训练 |

### 评分标准

| 分数 | 含义 |
|------|------|
| 5 分 | 近乎完美，可直接用于模型训练 |
| 4 分 | 高质量，适合模型训练 |
| 3 分 | 中等质量，需轻微调整后使用 |
| 2 分 | 存在明显问题，需修改后才能使用 |
| 1 分 | 质量较差，不建议用于训练 |
| 0 分 | 完全不适合训练 |

建议将 3.5 分作为可用阈值，低于 2 分的条目建议重新生成，评估结果可作为筛选和迭代优化的依据。系统支持批量评估，评估过程中会实时更新进度。

---

## 第八步：导出数据集

选择满意的条目，选择导出格式，下载微调训练数据集文件。

### 支持的导出格式

| 格式 | 说明 |
|------|------|
| JSON | 通用结构化格式，包含完整元数据 |
| JSONL | 每行一条 JSON 记录，适合流式加载 |
| CSV | 表格格式，便于在 Excel 中查看编辑 |
| Alpaca | LLaMA 系列模型的微调训练格式 |
| ShareGPT | ChatGPT 风格的对话训练格式 |

### 导出内容

| 字段 | 说明 |
|------|------|
| `id` | 数据集条目唯一标识符 |
| `question` | 问题内容 |
| `answer` | 答案内容 |
| `model` | 生成答案使用的模型名称 |
| `cot` | 思维链（可选） |
| `questionLabel` | 问题标签/分类 |
| `score` | AI 评估分数（若已评估） |

---

## 附录

### 任务系统

所有耗时操作均通过异步任务执行，用户可实时查看进度。

| 任务类型 | 描述 |
|----------|------|
| 文件处理 | PDF 转 Markdown 及文本分块 |
| 问题生成 | 从文本块生成问题列表 |
| 答案生成 | 为问题生成答案并创建数据集 |
| 数据清洗 | 清洗和优化已有数据 |
| 数据蒸馏 | 从已有数据中提炼新数据 |
| 数据评估 | 对数据集进行质量评估 |
| 模型评估 | 测试模型在评估集上的表现 |
| 盲测对比 | 两个模型的对比测试 |

**任务状态：** 0=待处理 / 1=已完成 / 2=失败 / 3=已中断

**并发控制：** 每种任务类型均可配置并发数（默认 2-3），支持中途取消。

### 模型评估（盲测对比）

系统支持对比不同模型在同一测试集上的表现：

1. 准备测试集：从数据集选择或自动生成评估题目（支持判断题、单选题、多选题、简答题、开放题）
2. 配置被测模型和评审模型
3. 系统将题目发送给被测模型，收集回答
4. 评审模型对每个回答进行评分
5. 汇总得分，生成评估报告

用户可为每种题型配置自定义评分锚点（Score Anchors），精确控制评审标准。支持两个模型盲测对比：将相同题目分别发送给模型 A 和模型 B，评审模型在不知道回答来源的情况下评分，最终比较综合得分。

---

*最后更新：2026-07-15*
